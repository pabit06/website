# Django – never commit (each host has its own)
*.pyc
__pycache__/
db.sqlite3
staticfiles/
media/

# Secrets & environment (never commit – set on each host)
.env
.env.*
!.env.example

# Virtual env (local only – server uses its own)
.venv
venv/
env/
ENV/
env.bak/
venv.bak/

# OS & editor
.DS_Store
*.log

# Optional / project artifacts
.gemini/
SUGGESTIONS.md
task.md
