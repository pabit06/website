# Copy to .env and adjust. .env is gitignored.

# Local development: use DEBUG=True so Django serves static files (no collectstatic needed).
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Production: set on the server (e.g. cPanel env vars), never commit.
# DEBUG=False
# SECRET_KEY=your-secret-key
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
